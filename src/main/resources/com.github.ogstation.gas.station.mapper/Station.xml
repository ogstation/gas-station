<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.github.ogstation.gas.station.domain.Station">
    <resultMap id="stationResultMap" type="Station">
        <id property="id" column="id" />
        <result property="name" column="name" />
        <result property="provinceCode" column="province_code" />
        <result property="cityCode" column="city_code" />
        <result property="countryCode" column="country_code" />
        <result property="addressDetails" column="address_details" />
        <result property="contact" column="contact" />
        <result property="telephone" column="telephone" />
        <result property="phone" column="phone" />
        <result property="email" column="email" />
        <result property="postcode" column="postcode" />
        <result property="homepage" column="homepage" />
    </resultMap>
    
    <insert id="insertStation" parameterType="Station">
        INSERT INTO station (id, name, province_code, city_code, country_code, address_details, contact, telephone, phone, email, postcode, homepage)
        VALUES (#{id}, #{name}, #{provinceCode}, #{cityCode}, #{countryCode}, #{addressDetails}, #{contact}, #{telephone}, #{phone}, #{email}, #{postcode}, #{homepage});
    </insert>

    <select id="getAllStations" resultMap="Station">
        SELECT *
        FROM station
    </select>

    <select id="getStationById" parameterType="String" resultMap="Station">
        SELECT *
        FROM station
        WHERE id = #{id};
    </select>

    <select id="getStationsByPageable" parameterType="Pageable" resultMap="Station">
        SELECT *
        FROM station
        LIMIT #{pageSize} OFFSET #{offset}
    </select>
    
    <update id="updateStation" parameterType="Station">
        UPDATE station
        SET
            name = #{name},
            province_code = #{provinceCode},
            city_code = #{cityCode},
            country_code = #{countryCode},
            address_details = #{addressDetails},
            contact = #{contact},
            telephone = #{telephone},
            phone = #{phone},
            email = #{email},
            postcode = #{postcode},
            homepage = #{homepage}
        WHERE id = #{id};
    </update>
    
    <delete id="deleteStation" parameterType="String">
        DELETE
        FROM station
        WHERE id = #{id};
    </delete>

</mapper>
